name: red_carga
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  http: ^1.1.0
  shared_preferences: ^2.2.2
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3
  url_launcher: ^6.2.2
  flutter_svg: ^2.0.9
  image_picker: ^1.0.7
  file_picker: ^8.1.2
  path_provider: ^2.1.1
  camera: ^0.11.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Assets (imágenes, iconos, etc.)
  assets:
    - assets/images/
    - assets/icons/
    - lib/features/deals/assets_temp/
